<%= simple_form_for @message do |f|%>
	<%= f.input :text, :as => :text, :label => "Mensagem", :input_html => { :cols => 50, :rows => 3 } %>
	<%= f.input :recipient_id, :as => :hidden, :input_html => {:value => @user.id}%>
	<%= f.submit "Enviar" %>
<% end%>