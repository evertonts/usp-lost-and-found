<div class="row ">
	<span class="<%= @item.lost? ? 'lost'  : 'found'  %> title">EDITAR ITEM</span>
	<hr class="separador <%= @item.lost? ? 'lost'  : 'found'  %>"/>
</div>

<div style="margin-top: 10px">
<%= render 'form' %>
</div>